apiVersion: v1
kind: Service
metadata:
  name: reservations
  labels:
    app: reservations
spec:
  selector:
    app: reservations
  type: LoadBalancer
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
      name: 'http'
