apiVersion: v1
kind: Service
metadata:
  name: auth-tcp
  labels:
    app: auth
spec:
  selector:
    app: auth
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
      name: 'tcp'
